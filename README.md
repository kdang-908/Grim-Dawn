LƯU Ý: LỖI BỊ GIỚI HẠN DUNG LƯỢNG KHI PUSH LÊN GIT
  //Dung de xoa Library khoi Git
- git rm -r --cached "My project/Library"
- git status (xem mình ở nhánh nào phải push lên khác nhánh main)
  // Dùng để tạo nhánh mới trước khi push -> với ... sẽ là tên mình đặt trước khi push. Tên đặt phải phù hợp với task mình làm
- git checkout -b ...
  // git add phai co dau "." o cuối
- git add .
  // ... dùng để ghi lại những gì mình đã làm
- git commit -m "..."
  // ... Tên nhánh
- git push --set-upstream origin ....
-------------------------------------------------------------------------------------------------------
- Art: dùng để cho model tướng, background,...
- Materials: dùng để thêm màu sắc, chất liệu ,....
- Scenes: màn hình các màn chơi. 
- Scripts: xử lí logic (code C#)
